extends layout

block content
    h1= title

    .delete-container
        h1(style='color: #333;') Confirm Deletion
        p.confirmation-message(style='color: #333; font-size: 18px; margin-bottom: 20px;') Are you sure you want to delete this supplier?

        form.delete-form(action=supplier.url + '/delete', method='POST')
            button.btn.btn-danger(type='submit') Delete

    a.back-link(href='/inventory/suppliers') Back to Suppliers
